# KAN-7738 — Resolução Técnica (Gemma 4 Dev Engine)

**👤 Desenvolvedor:** David Dev
**📅 Data:** 2026-07-23T19:01:54.672Z

## Solução de Engenharia
```python
from dataclasses import dataclass, field
from typing import Dict, Any, Optional
import uuid

# --- Domain Models ---
@dataclass
class WorkCard:
    """Representa un ticket o tarjeta de trabajo iniciada en el flujo.
    Immutable después de la creación inicial.
    """
    card_id: str = field(default_factory=lambda: str(uuid.uuid4()))
    title: str
    description: str
    status: str = "INITIATED"
    priority: str = "MEDIUM"
    assigned_to: Optional[str] = None
    created_at: str = field(default_factory=lambda: datetime.datetime.now(datetime.timezone.utc).isoformat())

class WorkflowError(Exception):
    """Excepción personalizada para errores en el flujo de trabajo.
    """
    pass

# --- Service Layer ---
class CardCreationService:
    """Servicio encargado de la creación y gestión inicial de Cards de Trabajo.
    """

    def __init__(self, data_store: Dict[str, WorkCard]):
        # Simulación de una base de datos o repositorio
        self._data_store = data_store

    def create_new_card(self, title: str, description: str, priority: str = "MEDIUM") -> WorkCard:
        """Inicia el flujo de trabajo creando una nueva tarjeta.

        Args:
            title: Título del trabajo.
            description: Descripción detallada del trabajo.
            priority: Nivel de prioridad (ej: LOW, MEDIUM, HIGH).

        Returns:
            La instancia de WorkCard recién creada.

        Raises:
            WorkflowError: Si los datos de entrada son inválidos.
        """
        if not title or not description:
            raise WorkflowError("El título y la descripción no pueden estar vacíos.")

        new_card = WorkCard(
            title=title,
            description=description,
    )
        # Lógica de validación de prioridad (ejemplo simple)
        if priority not in ["LOW", "MEDIUM", "HIGH"]:
             raise WorkflowError(f"Prioridad inválida: {priority}. Debe ser LOW, MEDIUM o HIGH.")

        self._data_store[new_card.card_id] = new_card
        print(f"Card creada exitosamente con ID: {new_card.card_id}")
        return new_card

    def get_card(self, card_id: str) -> Optional[WorkCard]:
        """Recupera una tarjeta por su ID.
        """
        return self._data_store.get(card_id)

# --- Ejemplo de Uso (Simulación del entorno)
from datetime import datetime

if __name__ == "__main__":
    # 1. Inicialización de datos (simulando la DB)
    mock_db: Dict[str, WorkCard] = {}
    service = CardCreationService(mock_db)

    try:
        # 2. Creación exitosa de un Card
        card1 = service.create_new_card(
            title="Implementar autenticación JWT",
            description="Configurar el endpoint de login y token usando JWT.",
            priority="HIGH"
        )
        print("--- Card 1 Creada ---")
        print(card1)

        # 3. Creación exitosa de otro Card
        card2 = service.create_new_card(
            title="Refactorizar módulo de logging",
            description="Mejorar la estructura del logger para manejar niveles de detalle.",
            priority="MEDIUM"
        )
        print("--- Card 2 Creada ---")
        print(card2)

        # 4. Verificación
        retrieved_card = service.get_card(card1.card_id)
        if retrieved_card:
            print("\nVerificación de Card 1:")
            print(f"ID: {retrieved_card.card_id}")
            print(f"Estado: {retrieved_card.status}")

    except WorkflowError as e:
        print(f"ERROR FATAL EN EL PROCESO: {e}")

```

## Diff de Código Gerado pelo Gemma 4
```diff
--- a/workflow_module.py
+++ b/workflow_module.py
@@ -0,0 +1,83 @@
+from dataclasses import dataclass, field
+from typing import Dict, Any, Optional
+import uuid
+from datetime import datetime
+
+# --- Domain Models ---
+@dataclass
+class WorkCard:
+    """Representa un ticket o tarjeta de trabajo iniciada en el flujo.
+    Immutable después de la creación inicial.
+    """
+    card_id: str = field(default_factory=lambda: str(uuid.uuid4()))
+    title: str
+    description: str
+    status: str = "INITIATED"
+    priority: str = "MEDIUM"
+    assigned_to: Optional[str] = None
+    created_at: str = field(default_factory=lambda: datetime.datetime.now(datetime.timezone.utc).isoformat())

+class WorkflowError(Exception):
+    """Excepción personalizada para errores en el flujo de trabajo.
+    """
+    pass
+
+# --- Service Layer ---
+class CardCreationService:
+    """Servicio encargado de la creación y gestión inicial de Cards de Trabajo.
+    """
+
+    def __init__(self, data_store: Dict[str, WorkCard]):
+        # Simulación de una base de datos o repositorio
+        self._data_store = data_store
+
+    def create_new_card(self, title: str, description: str, priority: str = "MEDIUM") -> WorkCard:
+        """Inicia el flujo de trabajo creando una nueva tarjeta.
+
+        Args:
+            title: Título del trabajo.
+            description: Descripción detallada del trabajo.
+            priority: Nivel de prioridad (ej: LOW, MEDIUM, HIGH).
+
+        Returns:
+            La instancia de WorkCard recién creada.
+
+        Raises:
+            WorkflowError: Si los datos de entrada son inválidos.
+        """
+        if not title or not description:
+            raise WorkflowError("El título y la descripción no pueden estar vacíos.")
+
+        new_card = WorkCard(
+            title=title,
+            description=description,
+        )
+
+        # Lógica de validación de prioridad (ejemplo simple)
+        if priority not in ["LOW", "MEDIUM", "HIGH"]:
+             raise WorkflowError(f"Prioridad inválida: {priority}. Debe ser LOW, MEDIUM o HIGH.")
+
+        self._data_store[new_card.card_id] = new_card
+        print(f"Card creada exitosamente con ID: {new_card.card_id}")
+        return new_card
+
    def get_card(self, card_id: str) -> Optional[WorkCard]:
+        """Recupera una tarjeta por su ID.
+        """
+        return self._data_store.get(card_id)
+
+# --- Ejemplo de Uso (Simulación del entorno) ---
+if __name__ == "__main__":
+    # 1. Inicialización de datos (simulando la DB)
+    mock_db: Dict[str, WorkCard] = {}
+    service = CardCreationService(mock_db)
+
+    try:
+        # 2. Creación exitosa de un Card
+        card1 = service.create_new_card(
+            title="Implementar autenticación JWT",
+            description="Configurar el endpoint de login y token usando JWT.",
+            priority="HIGH"
+        )
+        print("--- Card 1 Creada ---")
+        print(card1)
+
+        # 3. Creación exitosa de otro Card
+        card2 = service.create_new_card(
+            title="Refactorizar módulo de logging",
+            description="Mejorar la estructura del logger para manejar niveles de detalle.",
+            priority="MEDIUM"
+        )
+        print("--- Card 2 Creada ---")
+        print(card2)
+
+        # 4. Verificación
+        retrieved_card = service.get_card(card1.card_id)
+        if retrieved_card:
+            print("\nVerificación de Card 1:")
+            print(f"ID: {retrieved_card.card_id}")
+            print(f"Estado: {retrieved_card.status}
+            
+    except WorkflowError as e:
+        print(f"ERROR FATAL EN EL PROCESO: {e}")

```

## Cobertura de Testes
```python
import unittest
from datetime import datetime
# Importar las clases y el servicio simulado desde el archivo principal
# Asumiendo que las clases están disponibles en el mismo scope o se importan correctamente

# Re-definición de los modelos para asegurar que las pruebas sean autocontenidas si no se usa un módulo separado
from dataclasses import dataclass, field
import uuid

class WorkflowError(Exception):
    pass

@dataclass
class WorkCard:
    card_id: str = field(default_factory=lambda: str(uuid.uuid4()))
    title: str
    description: str
    status: str = "INITIATED"
    priority: str = "MEDIUM"
    assigned_to: Optional[str] = None
    created_at: str = field(default_factory=lambda: datetime.datetime.now(datetime.timezone.utc).isoformat())

class CardCreationService:
    def __init__(self, data_store: Dict[str, WorkCard]):
        self._data_store = data_store

    def create_new_card(self, title: str, description: str, priority: str = "MEDIUM") -> WorkCard:
        if not title or not description:
            raise WorkflowError("El título y la descripción no pueden estar vacíos.")

        new_card = WorkCard(
            title=title,
            description=description,
        )

        if priority not in ["LOW", "MEDIUM", "HIGH"]:
             raise WorkflowError(f"Prioridad inválida: {priority}. Debe ser LOW, MEDIUM o HIGH.")

        self._data_store[new_card.card_id] = new_card
        return new_card

    def get_card(self, card_id: str) -> Optional[WorkCard]:
        return self._data_store.get(card_id)

class TestCardCreation(unittest.TestCase):

    def setUp(self):
        # Configuración para cada test
        self.mock_db = {}
        self.service = CardCreationService(self.mock_db)

    def test_successful_card_creation(self):
        "Verifica que una tarjeta se crea correctamente con datos válidos.
        """
        title = "Test Title"
        description = "Test Description"
        priority = "HIGH"

        card = self.service.create_new_card(title, description, priority)

        # 1. Verificar que el objeto devuelto es una WorkCard
        self.assertIsInstance(card, WorkCard)
        # 2. Verificar que se almacenó en la DB simulada
        self.assertIn(card.card_id, self.mock_db)
        # 3. Verificar propiedades básicas
        self.assertEqual(card.title, title)
        self.assertEqual(card.priority, priority)
        self.assertEqual(card.status, "INITIATED")

    def test_creation_failure_empty_fields(self):
        "Verifica que se lanza WorkflowError si el título o la descripción están vacíos.
        """
        with self.assertRaisesRegex(WorkflowError, "El título y la descripción no pueden estar vacíos."):
            self.service.create_new_card("", "Descripción")

        with self.assertRaisesRegex(WorkflowError, "El título y la descripción no pueden estar vacíos."):
            self.service.create_new_card("Título", "")

    def test_creation_failure_invalid_priority(self):
        "Verifica que se lanza WorkflowError si la prioridad es inválida.
        """
        with self.assertRaisesRegex(WorkflowError, "Prioridad inválida: INVALID."):
            self.service.create_new_card("Title", "Desc", priority="INVALID")

    def test_card_retrieval(self):
        "Verifica que se puede recuperar una tarjeta por su ID.
        """
        # Crear una tarjeta válida primero
        valid_card = self.service.create_new_card("Retrieve Test", "Check retrieval")

        # Recuperar la tarjeta
        retrieved_card = self.service.get_card(valid_card.card_id)

        self.assertIsNotNone(retrieved_card)
        self.assertEqual(retrieved_card.title, "Retrieve Test")

        # Intentar recuperar un ID inexistente
        non_existent_card = self.service.get_card("fake-uuid-12345")
        self.assertIsNone(non_existent_card)

if __name__ == '__main__':
    unittest.main()
```